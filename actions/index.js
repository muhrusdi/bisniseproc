export const actionToggleLoad = (toggle) => {
  return {
    type: 'TOGGLE_LOAD',
    toggle: toggle
  }
}

export const actionToggleSide = (toggle) => {
  return {
    type: 'TOGGLE_SIDE',
    toggle: toggle
  }
}